---
const webTitle = 'Home page'
const num = 1
const str = "ryuta"
const obj = {
	name : str,
	number : num,
	favorites : ['astor', 'css', 'js']
}

const isVisible = true
---

<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title>Document</title>
</head>
<body>
	<ul>
		{isVisible ? obj.favorites.map(item => (
			<li>{item}</li>
		)) : <li>Not visible</li>}
	</ul>
	
</body>
</html>